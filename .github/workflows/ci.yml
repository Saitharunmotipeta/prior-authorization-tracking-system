name: CI Pipeline

on:
  push:
    branches:
      - saitharun

  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:

  build-test:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout Repository
        uses: actions/checkout@v4